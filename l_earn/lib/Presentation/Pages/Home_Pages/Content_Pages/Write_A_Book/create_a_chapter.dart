import 'package:flutter/material.dart';

class CreateAChapterPage extends StatelessWidget {
  const CreateAChapterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Create a chapter'),);
  }
}